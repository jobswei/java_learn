```mysql
create database db02;
use bd02;
create table users (id int , name varchar(255) , address varchar(255));
insert into users (1, 'fzm', 'sx');
insert into users (2, 'wzy', 'hb');
select * from users;
```

```mysql

```